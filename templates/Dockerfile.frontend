# Template Dockerfile for React (Vite) frontend
FROM node:20-alpine AS build
WORKDIR /app
COPY . .
RUN npm ci || npm install && npm run build

FROM nginx:alpine
# Serve static build
COPY --from=build /app/dist /usr/share/nginx/html
EXPOSE 3000
CMD [ "nginx", "-g", "daemon off;" ]
